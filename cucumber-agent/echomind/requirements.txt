# EchoMind 依赖清单

# ── AI / LLM ──────────────────────────────────────────────────────────────────
anthropic==0.40.0          # LLM 调用；意图 Embedding 分支支持远端客户端，缺失时本地兜底

# ── Web 框架 ──────────────────────────────────────────────────────────────────
fastapi==0.115.5
uvicorn[standard]==0.32.1
pydantic==2.10.3
python-multipart==0.0.12    # FastAPI 文件上传支持

# ── 数据存储 ──────────────────────────────────────────────────────────────────
redis==5.2.1
chromadb==0.5.23           # 向量数据库（长期记忆）

# ── 监控 ──────────────────────────────────────────────────────────────────────
prometheus-client==0.21.1

# ── HTTP 客户端 ───────────────────────────────────────────────────────────────
httpx==0.28.1              # monitor/performance_monitor.py webhook 告警

# ── 工具 ──────────────────────────────────────────────────────────────────────
python-dotenv==1.0.1

# ── RAG 中文向量（可选；缺失时知识库自动回退 Chroma 默认英文 embedding）─────────
# 对应 docs/评测报告_RAG对照实验_20260920.md 的 V4 口径；torch 请装 CPU 版：
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
transformers==4.45.2       # BGE bge-small-zh-v1.5 推理后端（BertModel + CLS pooling）
tokenizers==0.20.3         # 与 transformers 4.45 匹配的版本
safetensors==0.7.0         # BGE 权重格式
